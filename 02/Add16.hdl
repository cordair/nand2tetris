/**
    Add16 in two ways, RippleAdder and CarryLookAheadAdder
 */

CHIP Add16 {
    IN a[16], b[16];
    OUT out[16];

    PARTS:
    // RippleAdder16(a=a, b=b, out=out);
    CarryLookAheadAdder16(a=a, b=b, out=out);
    
}